# Layout

Terma uses a constraint-based layout system. Widgets can specify their dimensions as fixed sizes, automatic sizing based on content, or fractional sizes that distribute available space proportionally.
